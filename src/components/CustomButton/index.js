import './style.scss'
import {createElement} from "helpers/createElement";

/** Add button **/
const customButton = (click) => {
    return createElement('button', 'change-button', 'button')
}

export default customButton
